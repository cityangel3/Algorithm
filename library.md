# C++ library 
## 기본
```c++
    #include <iostream> // cout, cin etc ...
    #include <cstdio> // printf, fprintf, fopen etc ...
    using namespace std;
```
```c++
    /* c++, c 표준 스트림 동기화 끄기 */
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    /* 그냥 cstdio로 printf, scanf 쓰는게 더 빠른듯*/
```
# 자료구조
    * pair, vector, queue, stack, set, map, priority queue

## String
```c++
    #include <string>
    using namespace std;
    
```